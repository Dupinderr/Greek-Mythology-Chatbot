# Note: don't add --extra-index-url for CPU torch here. Streamlit Cloud resolves
# with uv, which only considers the first index carrying a package, so it breaks
# resolution for anything the PyTorch index also publishes (requests, etc).

# Agent + retrieval
langchain==1.3.14
langchain-core==1.5.1
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langgraph==1.2.9

# LLM (Groq) and local embeddings
langchain-groq==1.1.3
langchain-huggingface==1.2.2
sentence-transformers==5.6.1

# Vector store
langchain-chroma==1.1.0
chromadb==1.5.9

# Document loading
pypdf==6.14.2

# Web layer
fastapi==0.140.0
uvicorn[standard]==0.51.0
streamlit==1.60.0
requests==2.34.2
pydantic==2.13.4

# Config
python-dotenv==1.2.2
