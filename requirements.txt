# sentence-transformers pulls in torch, which defaults to the ~2GB CUDA build.
# Free hosts have no GPU and tight memory limits, so take the CPU-only wheel
# (~200MB) instead. Harmless locally — CPU is all this uses anyway.
--extra-index-url https://download.pytorch.org/whl/cpu

# Agent + retrieval
langchain==1.3.14
langchain-core==1.5.1
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langgraph==1.2.9

# LLM (Groq) and local embeddings
langchain-groq==1.1.3
langchain-huggingface==1.2.2
sentence-transformers==5.6.1

# Vector store
langchain-chroma==1.1.0
chromadb==1.5.9

# Document loading
pypdf==6.14.2

# Web layer
fastapi==0.140.0
uvicorn[standard]==0.51.0
streamlit==1.60.0
requests==2.34.2
pydantic==2.13.4

# Config
python-dotenv==1.2.2
