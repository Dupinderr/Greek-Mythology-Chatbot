# Secrets — never commit
.env
.env.*

# Virtualenv and caches
venv/
.hf_cache/
__pycache__/
*.pyc

# Rebuilt on first run from sources/, so not worth committing (~32MB)
chroma_db/

# The background ships as the .b64 text file beside it, since some hosts reject
# binaries that aren't in LFS. Keep the original local only.
assets/*.jpg
assets/*.png

# Unrelated to this project — left over from its earlier life as a paper Q&A bot
attention.pdf

.DS_Store
